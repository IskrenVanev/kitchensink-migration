package com.iskren;

/**
 * Placeholder — not used as a Spring Boot configuration anchor.
 *
 * Slice tests (@WebMvcTest, @DataJpaTest) reference
 * KitchensinkModernizedApplication directly via @SpringBootTest(classes=...)
 * to avoid TypeExcludeFilter conflicts caused by explicit @ComponentScan.
 */
class TestApplication {}
