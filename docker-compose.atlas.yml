services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kitchensink-app
    restart: unless-stopped
    environment:
      SERVER_PORT: "8080"
      MONGO_URL: ${MONGO_URL}
    ports:
      - "8081:8080"